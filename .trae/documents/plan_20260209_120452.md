I will fix the UI layout issues in `src/components/CoreGridSelector.tsx` by implementing the following changes:

1. **Fix Floating Window Cutoff**:

   * Use `ReactDOM.createPortal` to render the modal when it is not in embedded mode. This ensures the modal is attached directly to the `document.body`, preventing it from being clipped by parent containers or affected by stacking contexts (z-index) and transforms in the "Process Scanner" view.

2. **Fix Input Box Overflow**:

   * Add `min-w-0` to the "Interval (ms)" input field to ensures it shrinks properly within the flex container.

   * Adjust the container layout to handle narrow spaces more gracefully.

**File to modify**:

* `src/components/CoreGridSelector.tsx`

